export interface CategoryCsvRow {
    CategoryName: string;
    CourseNames: string;
    CategoryDescription: string;
}

export interface AddStructureData {
    courseName: string;
    header: string;
}